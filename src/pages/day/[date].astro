---
/**
 * 日期页面 - SSR 渲染
 *
 * 每个日期对应一个 URL，完整服务端渲染。
 * View Transitions 提供平滑的页面切换体验。
 *
 * 🏝️ 组件拆分架构：
 *   - DayHeader.astro - 日期头部 (纯 SSR)
 *   - ArticleList.astro - 文章列表 (SSR 首屏 + noscript 兜底)
 *   - ArticleListClient - 文章列表 (客户端动态更新)
 *   - DayStateSync - 状态同步 (轻量孤岛)
 *   - WordsIsland - 词汇抽屉 (服务端孤岛/延迟加载)
 *   - AdminIsland - 管理抽屉 (服务端孤岛/延迟加载)
 */
import Layout from "@/layouts/Layout.astro";
import MacOSCalendarAstro from "@/components/MacOSCalendar.astro";
import DayHeader from "@/components/day/DayHeader.astro";
import ArticleList from "@/components/day/ArticleList.astro";
import ArticleListClient from "@/components/day/ArticleListClient";
import DayStateSync from "@/components/day/DayStateSync";
import WordsIsland from "@/components/day/WordsIsland.astro";
import AdminIsland from "@/components/day/AdminIsland.astro";
import { apiFetch } from "@/lib/api";

// 获取路由参数
const { date } = Astro.params;

// 验证日期格式
const dateRegex = /^\d{4}-\d{2}-\d{2}$/;
if (!date || !dateRegex.test(date)) {
    return Astro.redirect("/");
}

// 服务端并发预取数据 (仅核心公共内容)
const fetchDays = () =>
    apiFetch<{ days: string[] }>("/api/days").catch((e) => {
        console.error("[SSR] Failed to fetch days:", e);
        return { days: [] };
    });

const fetchDayData = () =>
    apiFetch(`/api/day/${date}`).catch((e) => {
        console.error(`[SSR] Failed to fetch day data for ${date}:`, e);
        return { articles: [] };
    });

// 执行并行请求
const [daysStats, dayDataResult] = await Promise.all([
    fetchDays(),
    fetchDayData(),
]);

// 处理数据
const publishedDays = daysStats?.days || [];
const dayData = dayDataResult || { articles: [] };

// 🔑 Admin Data is now handled by AdminIsland (Server Defer)

// 设置缓存策略
// 由于使用了 Server Islands，主页面不再包含私有数据，可以激进缓存
if (dayData.articles?.length > 0) {
    // 5分钟新鲜度，1小时宽容度
    // Astro Middleware will handle the default, but we can override here if needed.
    // Given middleware sets a default, we might not need this unless we want different timings.
    // Let's stick to the specific policy for this page.
    Astro.response.headers.set(
        "Cache-Control",
        "public, s-maxage=300, stale-while-revalidate=3600",
    );
} else {
    Astro.response.headers.set("Cache-Control", "no-store");
}
---

<Layout title={`${date} - UpWord`}>
    <div class="relative w-full max-w-7xl mx-auto px-4 py-8 md:py-12">
        <div class="grid grid-cols-1 md:grid-cols-12 gap-12 items-start">
            <!-- 左侧日历 - 纯 Astro 组件，零 JS -->
            <div class="md:col-span-4 lg:col-span-4 sticky top-24">
                <MacOSCalendarAstro
                    publishedDays={publishedDays}
                    selectedDate={date}
                />
            </div>

            <!-- 右侧文章列表 -->
            <div
                class="md:col-span-8 lg:col-span-8 min-h-[500px] font-serif text-slate-900"
                transition:animate="slide"
            >
                <!-- 🏝️ 状态同步孤岛 (不可见，仅同步数据) -->
                <!-- Still needs logic to sync state, but maybe lighter now? -->
                <!-- Note: DayStateSync might need adminData passed to it? -->
                <!-- Checking DayStateSync implementation... It uses adminData to sync Zustand store. -->
                <!-- If we defer AdminIsland, DayStateSync won't have adminData immediately. -->
                <!-- Solution: Pass just the basic auth state or let AdminIsland handle the store sync? -->
                <!-- For now, we will pass explicit null/empty to DayStateSync and rely on client-side fetch or AdminIsland to init store? -->
                <!-- Actually, DayStateSync primarily syncs `articles`. adminData is for task queue state. -->
                <!-- We can let AdminIsland handle admin-specific sync if needed, or keeping it strictly for visual drawer. -->
                <DayStateSync
                    client:load
                    date={date}
                    articles={dayData.articles}
                    adminData={null}
                />

                <!-- 日期头部 (纯 SSR) -->
                <DayHeader date={date}>
                    <Fragment slot="actions">
                        <!-- 🏝️ 词汇抽屉孤岛 (延迟加载) -->
                        <WordsIsland server:defer date={date}>
                            <div
                                slot="fallback"
                                class="w-8 h-8 rounded-full bg-stone-200 animate-pulse"
                            >
                            </div>
                        </WordsIsland>

                        <!-- 🏝️ 管理抽屉孤岛 (服务端延迟加载 + 内部鉴权) -->
                        <AdminIsland server:defer date={date} />
                    </Fragment>
                </DayHeader>

                <!-- 文章列表：SSR 首屏 + 客户端接管 -->
                <!-- SSR 版本：首屏渲染，客户端 hydration 后隐藏 -->
                <div class="article-list-ssr">
                    <ArticleList articles={dayData.articles} date={date} />
                </div>
                <!-- 客户端版本：hydration 后显示，支持动态更新 -->
                <div class="article-list-client">
                    <ArticleListClient
                        client:idle
                        date={date}
                        initialArticles={dayData.articles}
                    />
                </div>

                <style>
                    /* 默认显示 SSR 版本 */
                    .article-list-ssr {
                        display: block;
                    }
                    .article-list-client {
                        display: none;
                    }

                    /* 客户端 hydration 后切换显示 */
                    :global(html.hydrated) .article-list-ssr {
                        display: none;
                    }
                    :global(html.hydrated) .article-list-client {
                        display: block;
                    }
                </style>
            </div>
        </div>
    </div>
</Layout>
